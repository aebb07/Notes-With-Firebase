<script>
/*import {firestoreDb} from './firebase';
   var searching= '';
   

 async function searchNote() {
      var refColl = await firestoreDb.collection('notes').where('title','==',searching)
      refColl.forEach(
      refDoc => {
         var document = refDoc.data()
         //...
      }
   )
   }*/

</script>

<!--div>
   <i id="icon" class="fas fa-search"></i>
	<input class="search" type="search" placeholder="Buscar en tus notas" bind:value={searching} on:input={()=>{searchNote(searching)}}>
</div-->

